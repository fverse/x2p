# Workspace rustfmt configuration.
#
# Only stable rustfmt options are used so the workspace formats consistently
# on the pinned 1.78 toolchain without `unstable_features`.

edition                  = "2021"
max_width                = 100
newline_style            = "Unix"
use_field_init_shorthand = true
