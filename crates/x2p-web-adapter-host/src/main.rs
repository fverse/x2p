//! Native-messaging host binary spawned by the browser extension.
//! Skeleton; the length-prefixed JSON loop is implemented in task 20.x.

fn main() {
    eprintln!("x2p-web-adapter-host: not yet implemented (Phase 1, task 20.1)");
    std::process::exit(1);
}
