//! The `x2p` command-line binary entry point. Skeleton; the clap subcommand
//! tree is wired in task 15.x.

fn main() {
    // The CLI surface is implemented in later tasks. This placeholder lets the
    // workspace build cleanly (Req. 25.1) without committing to any subcommand
    // shape ahead of task 15.1.
    eprintln!("x2p: cli not yet implemented (Phase 1, task 15.1)");
    std::process::exit(1);
}
